import {styled} from "@mui/system";

export // Custom styled image component to make it responsive
const ResponsiveImage = styled('img')({
    width: '100%',
    height: 'auto',
    display: 'block',
    marginTop: '32px',
    marginBottom: "32px",
});